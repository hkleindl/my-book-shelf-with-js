<%= form_for @book do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :author %>
  <%= f.text_field :author %><br>

  <%= fields_for :ratings do |rating| %>
    <%= rating.label :stars %>
    <%= rating.number_field :stars %><br>
  <% end %>

  <%= f.submit %>
<% end %>